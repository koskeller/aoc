#![allow(unused)]

use utils::Grid;

fn main() {
    let s = include_str!("input").to_owned();
    println!("{}", part_one(s.clone()));
    println!("{}", part_two(s));
}

fn part_one(input: String) -> usize {
    let mut answer = 0;

    answer
}

fn part_two(input: String) -> usize {
    let mut answer = 0;

    answer
}
